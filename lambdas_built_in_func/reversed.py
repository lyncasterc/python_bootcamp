# reversed - returns a reverse iterator

nums = [1,2,3,4,5]



print(list(reversed(nums))) #returns a reverse of the list, although there is already a reverse list method that would be more applicable

for x in reversed(range(0,9)):
  print(x) #reverses numbers in the range