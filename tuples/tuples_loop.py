names = ('Colt', 'Blue', 'Rusty', 'Lassie')

for name in names:
  print(name + '', 'smells')


# methods 

x = (1,2,2,3)

# counts the number of times a value is in the tuple
print(x.count(2)) #returns 2

# give the index of the given value within the index
print(x.index(1)) #returns 0

